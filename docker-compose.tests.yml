version: '3.5'

services:
  gponmonitor.tests:
    image: ${DOCKER_REGISTRY}gponmonitor.tests:1.0.4
    volumes:
      - /docker/GPONMonitor/temp:/var/temp
    build:
      context: .
      dockerfile: ./Dockerfile.tests
